model:
  base: Qwen/Qwen3-0.6B

training:
  fine_tune_type: lora
  iterations: 200
  batch_size: 2
  num_layers: 8
  learning_rate: 1e-5
  max_seq_length: 512
  mask_prompt: true
  save_every: 50
  steps_per_report: 10
  steps_per_eval: 20
